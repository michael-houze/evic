@model IEnumerable<MVC_DATABASE.Models.RFP>

@{
    ViewBag.Title = "RFP Home";
}
                      <center>
                          <h1>RFP Home</h1>

                          <p>
                              @Html.ActionLink("Create New RFP", "Create")
                          </p>
                      </center>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CATEGORY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CREATED)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EXPIRES)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CATEGORY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CREATED)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EXPIRES)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RFPID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RFPID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RFPID })
        </td>
    </tr>
}

</table>
<center>
                      <p>
                          @Html.ActionLink("Create New RFP", "Create")
                      </p>
</center>
